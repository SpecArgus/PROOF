[project]
name = "proof-contracts"
version = "0.1.0"
description = "Language-neutral versioned contracts for PROOF results"
requires-python = ">=3.14,<3.15"
dependencies = []

[build-system]
requires = ["hatchling==1.31.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/proof_contracts"]
