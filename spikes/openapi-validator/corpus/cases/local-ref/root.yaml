openapi: 3.0.3
info:
  title: Valid local reference fixture
  version: 1.0.0
paths:
  /pets:
    get:
      operationId: getPets
      responses:
        "200":
          description: Pet list
          content:
            application/json:
              schema:
                $ref: "./components.yaml#/components/schemas/PetList"
