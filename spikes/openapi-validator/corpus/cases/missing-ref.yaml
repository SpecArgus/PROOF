openapi: 3.1.0
info:
  title: Missing local reference fixture
  version: 1.0.0
paths:
  /pets:
    get:
      operationId: getPets
      responses:
        "200":
          description: Pet list
          content:
            application/json:
              schema:
                $ref: "./does-not-exist.yaml#/components/schemas/PetList"
