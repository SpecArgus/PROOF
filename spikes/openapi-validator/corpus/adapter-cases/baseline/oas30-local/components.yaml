components:
  schemas:
    Pet:
      type: object
      required:
        - id
      properties:
        id:
          type: integer
          format: int64
    PetList:
      type: array
      items:
        $ref: "#/components/schemas/Pet"
