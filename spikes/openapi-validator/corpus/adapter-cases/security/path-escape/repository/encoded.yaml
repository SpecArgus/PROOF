openapi: 3.0.3
info:
  title: Percent-encoded path escape
  version: 1.0.0
paths:
  /pets:
    get:
      responses:
        "200":
          description: Escaped response
          content:
            application/json:
              schema:
                $ref: "%2e%2e/outside.yaml"
