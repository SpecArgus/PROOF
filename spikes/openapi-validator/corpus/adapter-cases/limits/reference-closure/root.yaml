openapi: 3.0.3
info:
  title: Reference closure accounting
  version: 1.0.0
paths:
  /value:
    get:
      responses:
        "200":
          description: Value
          content:
            application/json:
              schema:
                $ref: "./one.yaml"
