openapi: 3.1.0
info:
  title: Reference chain over depth limit
  version: 1.0.0
paths:
  /value:
    get:
      responses:
        "200":
          description: Value
          content:
            application/json:
              schema:
                $ref: "./level-1.yaml"
